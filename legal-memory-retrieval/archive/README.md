# Archive

Code kept for reference only. **Not served, not built, not copied into the image.**

- `legacy-static-ui/` — the pre-React static UI (`app.js`, `styles.css`). It wrote API
  text into `innerHTML` (see production-readiness review P0-05). Moved out of `static/`
  on 2026-09-24 so the SPA handler can no longer serve it.
