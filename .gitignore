# Secrets — never commit API keys or DB credentials
.env
.env.local
.env.*.local

# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
.pytest_cache/

# OS / editor
.DS_Store
.code-review-graph/

# Local model / embedding caches (re-download on setup)
.cache/
*.pt
*.bin
